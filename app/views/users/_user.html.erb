<li>
  <%= link_to user.name, user %>
  <% if current_user.tipo_user_id == "Admin" %>
  |
  <%= link_to "delete", user, :method => :delete, :confirm => "You sure?",
                              :title => "Delete #{user.name}" %>
  <% end %>
</li>